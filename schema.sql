CREATE TABLE `program_report` (
    `university`    TEXT,
    `university_link`    TEXT,
    `state`    TEXT,
    `cae-cd`    INTEGER,
    `cae-2y`    INTEGER,
    `cae-r`     INTEGER,
    `cae-co`    INTEGER,
    `research_center`   TEXT,
    `security_cs`    INTEGER,
    `cs_curr`    TEXT,
    `security_se`    INTEGER,
    `se_curr`    TEXT,
    `security_is`    INTEGER,
    `is_curr`    TEXT,
    `security_it`    INTEGER,
    `it_curr`    TEXT,
    `security_ce`  INTEGER,
    `ce_curr`    TEXT,
    `security_csec`  INTEGER,
    `csec_curr`  TEXT,
    `sec_dept`   INTEGER,
    `security_minor`  INTEGER,
    `minor_curr`    TEXT,
    `security_masters` INTEGER,
    `masters_curr`  TEXT,
    `gen_cyber`      INTEGER,
    `ccdc_2017`      INTEGER,
    `ccdc_2016`      INTEGER,
    `ccdc_2015`      INTEGER,
    `ccdc_2014`      INTEGER,
    `ccdc_2013`      INTEGER,
    `ccdc_2012`      INTEGER,
    `ccdc_2011`      INTEGER,
    `ccdc_2010`      INTEGER,
    `ccdc_2009`      INTEGER,
    `ccdc_2008`      INTEGER,
    `ccdc_2007`      INTEGER,
    `ccdc_2006`      INTEGER,
    `ccdc_2005`      INTEGER
);
